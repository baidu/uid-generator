spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:3306/zr?useUnicode=true&characterEncoding=utf8&autoReconnect=true&connectTimeout=2000&socketTimeout=2000&&serverTimezone=Asia/Shanghai

mybatis:
  mapper-locations: classpath:/META-INF/mybatis/mapper/WORKER*.xml
  type-aliases-package: com.baidu.fsg.uid.worker.dao


uid:
  enable: true
